fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
bcrypt==4.2.1
openai==3.3.1

# torch must be installed from the CPU-only index first, or pip will pull
# the much larger CUDA build from PyPI by default:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
transformers==5.15.1
pyjwt==2.13.0
